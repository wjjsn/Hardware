# Hardware 开发约束

Hardware 是 HAL 之上的设备驱动层。新增代码和修改旧代码都必须遵循本文件。

## 代码风格

- 公共代码放在 `namespace Hardware`。
- 类型使用 PascalCase，方法和变量使用 `snake_case`。
- 文件名使用小写 `snake_case`，每个头文件使用 `#pragma once`。
- 使用 C++17、`<stdint.h>` 和 `uint*_t`。
- 使用 `enum class`，初始化所有成员，禁止 C 风格转换和动态内存。
- 只读参数使用 `const`；公开索引、长度和坐标必须检查边界。

## 驱动规则

- 驱动只通过 `subprojects/HAL/AGENTS.md` 中的接口访问硬件。
- 禁止包含厂商 SDK、访问 MCU 寄存器或依赖平台私有宏。
- 模板参数按职责命名，如 `i2c_device`、`spi_device`、`gpio`、`timer`、`pwm`、`transport`。
- `init()` 必须把设备和内部状态初始化到确定状态。
- 读取、写入、配置和中断方法分别使用 `read_*`、`write_*`、`set_*`、`*_isr`。
- 文件头简要说明芯片、总线、设备地址和寄存器字节序。

## 总线使用

- I2C 地址一律为未移位的 7-bit 地址，寄存器地址为 8-bit，访问使用 HAL 的统一四参数接口。
- SPI 单次事务使用普通收发；同一 CS 下的多次收发使用 `select()`、`*_without_ctl_select()`、`deselect()`。
- 手动控制 CS 时，退出前必须恢复 `deselect()`。
- 不得在驱动内兼容多套 HAL 接口；先修改 HAL，使其符合统一接口。

## 安全要求

- 位操作统一使用 `BIT::SET`、`BIT::CLR`、`BIT::TGL`、`BIT::READ`，禁止隐式外部宏。
- 缓冲区必须说明方向和长度，不得越界，也不得在同步调用结束后保存其指针。
- 禁止未限制次数的忙等。
- ISR 与主循环共享的数据必须有明确的同步措施；ISR 中不得阻塞或格式化输出。
- 公共头文件必须可独立包含，不依赖包含顺序或未声明的外部文件。

## 修改旧代码

- 修改旧驱动时同步统一命名空间、命名、类型、初始化、const 和边界检查。
- 修复错误优先，不为旧接口新增兼容包装。
- 不同设备或显示规格不得重复定义同名公共类型。
- 每个修改后的驱动至少进行一次独立包含和实例化编译检查。
